import React from "react";
import ReactDOM from "react-dom/client";
import { NavisApp } from "./NavisApp";
import "./navis.css";
ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <NavisApp />
  </React.StrictMode>,
);
