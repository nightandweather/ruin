import{useMemo,useState}from"react";import{assessFailure,FAILURE_PLANS,plansFor,SYSTEM_META,type FailureSeverity,type RuinSystem}from"./sentinel";
const systems=Object.keys(SYSTEM_META)as RuinSystem[];const severities:FailureSeverity[]=["minor","major","critical"];
export function SentinelApp(){const[system,setSystem]=useState<RuinSystem>("helios");const[planId,setPlanId]=useState(FAILURE_PLANS[0].id);const[severity,setSeverity]=useState<FailureSeverity>("major");const plans=plansFor(system);const plan=FAILURE_PLANS.find(p=>p.id===planId&&p.system===system)??plans[0];const assessment=useMemo(()=>assessFailure(plan,severity),[plan,severity]);const chooseSystem=(s:RuinSystem)=>{setSystem(s);setPlanId(plansFor(s)[0].id)};return <main className="sn-shell"><header className="sn-top"><div className="sn-brand"><span>S//N</span><div><strong>RUIN // SENTINEL</strong><small>SYSTEM-WIDE FAULT RESPONSE</small></div></div><nav><a href="/">HELIOS</a><a href="/navis.html">NAVIS</a><a href="/ignis.html">IGNIS</a><a href="/mender.html">MENDER</a><b>SENTINEL</b></nav><div className="sn-state"><span>{FAILURE_PLANS.length} PLANS · {systems.length} SYSTEMS</span><b className={assessment.status.toLowerCase()}>{assessment.status}</b></div></header><section className="sn-layout">
<aside className="sn-panel sn-systems"><Title n="01" text="SYSTEM COVERAGE"/><div>{systems.map(s=><button key={s} className={system===s?"active":""} onClick={()=>chooseSystem(s)}><i>{SYSTEM_META[s].code}</i><span><b>{SYSTEM_META[s].name}</b><small>{SYSTEM_META[s].purpose}</small></span><em>{plansFor(s).length}</em></button>)}</div><p><b>REGISTRY CONTRACT</b>Every executable RUIN module has detection, isolation, safe state, fallback, human decision, recovery evidence, and a safety invariant.</p></aside>
<section className="sn-panel sn-map"><div className="sn-maphead"><Title n="02" text="FAILURE TOPOLOGY"/><span>{SYSTEM_META[system].name} · {plans.length} VERIFIED PLANS</span></div><div className="sn-failures">{plans.map((p,i)=><button key={p.id} className={plan.id===p.id?"active":""} onClick={()=>setPlanId(p.id)}><span>F-{String(i+1).padStart(2,"0")}</span><b>{p.name}</b><small>{p.cause}</small><i>{p.dependencies.map(d=>SYSTEM_META[d].code).join(" · ")||"LOCAL"}</i></button>)}</div><div className="sn-graph"><svg viewBox="0 0 720 330" role="img" aria-label="Fault propagation and containment diagram"><defs><marker id="snArrow" markerWidth="9" markerHeight="7" refX="8" refY="3.5" orient="auto"><path d="M0 0L9 3.5 0 7Z" fill="#de6670"/></marker></defs><g className="sn-origin"><circle cx="360" cy="165" r="65"/><circle cx="360" cy="165" r="48"/><text x="360" y="158">{SYSTEM_META[system].code}</text><text x="360" y="177">{plan.name.toUpperCase()}</text></g>{plan.dependencies.map((d,i)=>{const a=i/Math.max(1,plan.dependencies.length)*Math.PI*2-Math.PI/2,x=360+Math.cos(a)*225,y=165+Math.sin(a)*120;return <g key={d}><path className="sn-link" d={`M360 165L${x} ${y}`} markerEnd="url(#snArrow)"/><circle className="sn-dep" cx={x} cy={y} r="33"/><text className="sn-code" x={x} y={y+3}>{SYSTEM_META[d].code}</text></g>})}<path className="sn-wall" d="M238 286H482"/><text className="sn-wall-label" x="360" y="308">CONTAINMENT BOUNDARY · {plan.safeState}</text></svg><div className="sn-blast"><span>ESTIMATED BLAST RADIUS</span><b>{assessment.blastRadius}%</b><i><em style={{width:`${assessment.blastRadius}%`}}/></i><small>{plan.dependencies.length} DIRECT DEPENDENC{plan.dependencies.length===1?"Y":"IES"}</small></div></div></section>
<aside className="sn-panel sn-detail"><Title n="03" text="INCIDENT ASSESSMENT"/><div className="sn-severity">{severities.map(s=><button key={s} className={severity===s?"active":""} onClick={()=>setSeverity(s)}>{s.toUpperCase()}</button>)}</div><div className={`sn-verdict ${assessment.status.toLowerCase()}`}><span>RISK PRIORITY</span><b>{assessment.riskPriority}</b><small>{assessment.status} · SAFE STATE IN ≤ {assessment.safeSeconds}s</small></div><dl><div><dt>CAUSE</dt><dd>{plan.cause}</dd></div><div><dt>DETECTOR</dt><dd>{plan.detector}</dd></div><div><dt>SAFE STATE</dt><dd>{plan.safeState}</dd></div><div><dt>AUTOMATION</dt><dd>{assessment.automationPercent}%</dd></div></dl><Title n="04" text="EARLY INDICATORS"/><div className="sn-precursors">{plan.precursors.map((x,i)=><span key={x}><i>0{i+1}</i>{x}</span>)}</div><Title n="05" text="SAFETY INVARIANT"/><blockquote>{plan.invariant}</blockquote><div className="sn-human"><span>HUMAN DECISION</span><p>{plan.humanDecision}</p></div></aside>
<section className="sn-panel sn-response"><div className="sn-timeline"><Title n="06" text="AUTOMATED RESPONSE TIMELINE"/><div>{assessment.timeline.map((e,i)=><article key={e.phase} className={i===2?"safe":""}><time>T+{e.at}s</time><i/><span><b>{e.phase}</b><small>{e.action}</small></span></article>)}</div></div><div className="sn-actions"><Title n="07" text="ISOLATION ACTIONS"/><ol>{plan.automatic.map(x=><li key={x}>{x}</li>)}</ol><div><span>FALLBACK OPERATING MODE</span><b>{plan.fallback}</b></div></div><div className="sn-recovery"><Title n="08" text="RECOVERY GATE"/><p>No reset is accepted from elapsed time alone. Both evidence gates must pass.</p>{plan.recovery.map((x,i)=><article key={x}><span>GATE {i+1}</span><b>{x}</b><i/></article>)}<button>HOLD UNTIL EVIDENCE VERIFIED</button></div></section>
</section></main>}
function Title({n,text}:{n:string;text:string}){return <div className="sn-title"><span>{n}</span>{text}</div>}
