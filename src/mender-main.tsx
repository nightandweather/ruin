import React from "react";
import ReactDOM from "react-dom/client";
import { MenderApp } from "./MenderApp";
import "./mender.css";
ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <MenderApp />
  </React.StrictMode>,
);
