import{useMemo,useState}from"react";import{evaluateMnemosyne,mnemosyneConfig,type CaptureMode,type MnemosyneConfig,type MnemosyneIncident}from"./mnemosyne";const f=(v:number,d=1)=>v.toLocaleString(undefined,{maximumFractionDigits:d});
export function MnemosyneApp(){const[c,setC]=useState<MnemosyneConfig>(()=>mnemosyneConfig()),r=useMemo(()=>evaluateMnemosyne(c),[c]);const u=<K extends keyof MnemosyneConfig>(k:K,v:MnemosyneConfig[K])=>setC(x=>({...x,[k]:v}));return <main className="mn-shell"><header className="mn-top"><div className="mn-brand"><span>M//N</span><div><strong>RUIN // MNEMOSYNE</strong><small>NEURAL IDENTITY EVIDENCE LABORATORY</small></div></div><nav><a href="/datacore.html">DATACORE</a><a href="/genesis.html">GENESIS</a><b>MNEMOSYNE</b><a href="/sentinel.html">SENTINEL</a></nav><div className="mn-state">NO CLAIM OF CONSCIOUSNESS TRANSFER · <b className={r.readiness.toLowerCase()}>{r.readiness}</b></div></header><section className="mn-layout"><aside className="mn-panel mn-left"><Title n="01" t="CAPTURE METHOD"/><div className="mn-modes">{(["non-invasive","implant-array","destructive-em"]as CaptureMode[]).map(x=><button key={x} className={c.captureMode===x?"active":""} onClick={()=>u("captureMode",x)}><b>{x.replace("-"," ").toUpperCase()}</b><small>{x==="non-invasive"?"LIVE / LOW RESOLUTION":x==="implant-array"?"LIVE / LOCAL DYNAMICS":"SYNAPSE MAP / DESTRUCTIVE"}</small></button>)}</div><Title n="02" t="BIOLOGICAL COVERAGE"/><Range label="STRUCTURAL MAP" value={c.structuralCoveragePercent} change={v=>u("structuralCoveragePercent",v)}/><Range label="SYNAPSE RESOLUTION" value={c.synapseResolutionPercent} change={v=>u("synapseResolutionPercent",v)}/><Range label="DYNAMIC STATE" value={c.dynamicStatePercent} change={v=>u("dynamicStatePercent",v)}/><Range label="GLIAL + METABOLIC" value={c.glialStatePercent} change={v=>u("glialStatePercent",v)}/><Range label="MOLECULAR STATE" value={c.molecularStatePercent} change={v=>u("molecularStatePercent",v)}/></aside><section className="mn-panel mn-stage"><div className="mn-stagehead"><Title n="03" t="IDENTITY EVIDENCE GRAPH"/><span>{f(r.evidenceCoverage)}% STATE COVERAGE · {f(r.missingInformationPercent)}% UNKNOWN</span></div><Brain r={r}/><div className="mn-bars"><Bar l="STRUCTURE" v={r.structural}/><Bar l="SYNAPSES" v={r.synaptic}/><Bar l="DYNAMICS" v={r.dynamic}/><Bar l="GLIA" v={r.glial}/><Bar l="MOLECULAR" v={r.molecular}/></div></section><aside className="mn-panel mn-right"><Title n="04" t="IDENTITY VERDICT"/><div className={`mn-verdict ${r.readiness.toLowerCase()}`}><span>ACTIVATION AUTHORITY</span><b>{r.identityStatus}</b><small>{r.activationAllowed?"ACTIVATION MAY PROCEED ONLY AS AN EVIDENCE CANDIDATE":r.constraints[0]??"ACTIVATION BLOCKED"}</small></div><div className="mn-metrics"><Metric l="EVIDENCE COVERAGE" v={f(r.evidenceCoverage)} u="%"/><Metric l="VALIDATION CONF." v={f(r.validationConfidence)} u="%"/><Metric l="DIVERGENCE" v={f(r.divergencePercent)} u="%" w={r.divergencePercent>15}/><Metric l="INSTANCES" v={String(c.instances)} u=""/><Metric l="MEMORY MATCH" v={f(r.memory)} u="%"/><Metric l="BEHAVIOR MATCH" v={f(r.behavior)} u="%"/></div><label className="mn-consent"><input type="checkbox" checked={c.consentVerified} onChange={e=>u("consentVerified",e.target.checked)}/><span><b>VERIFIED INFORMED CONSENT</b><small>Capture, activation, forks, retention, and deletion are separate authorities.</small></span></label><Title n="05" t="INCIDENT"/><div className="mn-incidents">{(["none","state-corruption","sensory-loss","fork-conflict"]as MnemosyneIncident[]).map(x=><button key={x} className={c.incident===x?"active":""} onClick={()=>u("incident",x)}>{x.replace("-"," ").toUpperCase()}</button>)}</div></aside><section className="mn-panel mn-bottom"><div><Title n="06" t="VALIDATION PROTOCOL"/><Step l="RECORDING WINDOW" v={c.recordingHours} min={1} max={10000} step={24} unit="h" change={v=>u("recordingHours",v)}/><Step l="MEMORY PROBES" v={c.memoryProbes} min={0} max={5000} step={50} change={v=>u("memoryProbes",v)}/><Range label="MEMORY AGREEMENT" value={c.memoryAgreementPercent} change={v=>u("memoryAgreementPercent",v)}/><Range label="BEHAVIOR AGREEMENT" value={c.behaviorAgreementPercent} change={v=>u("behaviorAgreementPercent",v)}/></div><div><Title n="07" t="EMBODIMENT + TIME"/><Range label="SENSORY EMBODIMENT" value={c.sensoryEmbodimentPercent} change={v=>u("sensoryEmbodimentPercent",v)}/><Step l="ELAPSED SINCE CAPTURE" v={c.elapsedYears} min={0} max={100} step={1} unit="yr" change={v=>u("elapsedYears",v)}/><Step l="ACTIVE INSTANCES" v={c.instances} min={1} max={20} step={1} change={v=>u("instances",v)}/><label className="mn-original"><input type="checkbox" checked={c.originalAlive} onChange={e=>u("originalAlive",e.target.checked)}/>BIOLOGICAL ORIGINAL ALIVE</label></div><div className="mn-rules"><Title n="08" t="PERSONHOOD INVARIANTS"/><p>01 · Similar behavior is evidence, not proof of continuity.</p><p>02 · Every fork receives independent authority and consent.</p><p>03 · No instance may authorize deletion of another.</p><p>04 · Missing embodiment blocks unsupervised activation.</p></div><div className="mn-register"><Title n="09" t="EVIDENCE GAPS"/>{r.constraints.map((x,i)=><p key={x}><span>G-{i+1}</span>{x}</p>)}<small>RESEARCH SIMULATOR · NOT MEDICAL SOFTWARE · NOT A MIND UPLOAD</small></div></section></section></main>}
function Brain({r}:{r:ReturnType<typeof evaluateMnemosyne>}){const nodes=Array.from({length:34},(_,i)=>({x:220+Math.cos(i*2.399)*Math.sqrt(i)*33,y:170+Math.sin(i*2.399)*Math.sqrt(i)*24}));return <svg className="mn-brain" viewBox="0 0 620 340" role="img" aria-label="Layered neural evidence graph"><path className="outline" d="M115 184C80 85 189 38 280 67C354 14 474 66 478 142C545 183 494 283 407 279C341 324 237 294 205 273C121 283 71 226 115 184Z"/>{nodes.slice(1).map((n,i)=><line key={i} x1={n.x} y1={n.y} x2={nodes[Math.floor(i/2)].x} y2={nodes[Math.floor(i/2)].y}/>) }{nodes.map((n,i)=><circle key={i} cx={n.x} cy={n.y} r={i%5===0?5:3}/>) }<g className="scan"><path d="M80 105H520M80 145H520M80 185H520M80 225H520"/><text x="510" y="312">OBSERVED ≠ PRESERVED ≠ CONTINUOUS</text></g><circle className="coverage" cx="300" cy="170" r={Math.max(12,r.evidenceCoverage*1.2)}/></svg>}
function Title({n,t}:{n:string;t:string}){return <div className="mn-title"><span>{n}</span>{t}</div>}function Range({label,value,change}:{label:string;value:number;change:(v:number)=>void}){return <label className="mn-range"><span>{label}<b>{f(value)}%</b></span><input aria-label={label} type="range" min={0} max={100} step={1} value={value} onChange={e=>change(+e.target.value)}/></label>}function Step({l,v,min,max,step,unit="",change}:{l:string;v:number;min:number;max:number;step:number;unit?:string;change:(v:number)=>void}){return <div className="mn-step"><span>{l}</span><div><button aria-label={`Decrease ${l}`} onClick={()=>change(Math.max(min,v-step))}>−</button><b>{f(v,0)}<small>{unit}</small></b><button aria-label={`Increase ${l}`} onClick={()=>change(Math.min(max,v+step))}>+</button></div></div>}function Metric({l,v,u,w}:{l:string;v:string;u:string;w?:boolean}){return <div className={`mn-metric ${w?"warning":""}`}><span>{l}</span><b>{v}<small>{u}</small></b></div>}function Bar({l,v}:{l:string;v:number}){return <div><span>{l}<b>{f(v)}%</b></span><i><em style={{width:`${v}%`}}/></i></div>}
