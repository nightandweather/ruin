import{useMemo,useState}from"react";import{evaluateMender,menderConfig,MENDER_PRESETS,TASKS,type MenderConfig,type MenderFrame,type RepairTask}from"./mender";
const frames:Record<MenderFrame,[string,string]>={"free-flyer":["FREE-FLYER","ORBITAL APPROACH"],"rail-walker":["RAIL WALKER","HULL-ANCHORED"],"lunar-crawler":["LUNAR CRAWLER","SURFACE INDUSTRY"],"micro-swarm":["MICRO SWARM","COOPERATIVE INSPECTION"]};const fmt=(v:number,d=1)=>v.toLocaleString(undefined,{maximumFractionDigits:d});
export function MenderApp(){const[config,setConfig]=useState<MenderConfig>(()=>menderConfig());const result=useMemo(()=>evaluateMender(config),[config]);const update=<K extends keyof MenderConfig>(k:K,v:MenderConfig[K])=>setConfig(c=>({...c,[k]:v}));const frame=(f:MenderFrame)=>setConfig({frame:f,task:config.task,signalDelayS:config.signalDelayS,...MENDER_PRESETS[f]});return <main className="md-shell"><header className="md-top"><div className="md-brand"><span>M//R</span><div><strong>RUIN // MENDER</strong><small>ROBOTIC REPAIR ARCHITECT</small></div></div><nav><a href="/">HELIOS</a><a href="/progenitor.html">PROGENITOR</a><a href="/navis.html">NAVIS</a><b>MENDER</b><a href="/sentinel.html">SENTINEL</a></nav><div className="md-state"><span>ISAM MAINTENANCE TWIN</span><b className={result.readiness.toLowerCase()}>{result.readiness}</b></div></header><section className="md-layout">
<aside className="md-panel md-config"><Title n="01" text="MOBILITY FRAME"/><div className="md-frames">{(Object.keys(frames)as MenderFrame[]).map(f=><button key={f} className={config.frame===f?"active":""} onClick={()=>frame(f)}><b>{frames[f][0]}</b><small>{frames[f][1]}</small></button>)}</div><Title n="02" text="REPAIR CONTRACT"/><div className="md-tasks">{(Object.keys(TASKS)as RepairTask[]).map(t=><button key={t} className={config.task===t?"active":""} onClick={()=>update("task",t)}>{TASKS[t].name}<small>{TASKS[t].forceN} N · {TASKS[t].precisionMm} mm</small></button>)}</div><Title n="03" text="MANIPULATOR"/><Control label="ARM COUNT" value={config.armCount} unit="arms" min={1} max={6} step={1} change={v=>update("armCount",v)}/><Control label="ARM REACH" value={config.armReachM} unit="m" min={.4} max={8} step={.2} change={v=>update("armReachM",+v.toFixed(1))}/><Control label="DOF / ARM" value={config.dofPerArm} unit="DOF" min={3} max={9} step={1} change={v=>update("dofPerArm",v)}/><Control label="PAYLOAD RATING" value={config.payloadKg} unit="kg" min={2} max={500} step={5} change={v=>update("payloadKg",v)}/><Control label="GRIP FORCE" value={config.gripForceN} unit="N" min={50} max={5000} step={50} change={v=>update("gripForceN",v)}/></aside>
<section className="md-panel md-stage"><div className="md-stagehead"><Title n="04" text="PHYSICAL SERVICE TWIN"/><span>{frames[config.frame][0]} · {TASKS[config.task].name}</span></div><RobotDiagram config={config} readiness={result.readiness}/><div className="md-anatomy"><Card code="A" name="ANCHOR LIMB" text={`${fmt(result.anchorMomentNm,0)} N·m restraint`}/><Card code="B" name="SERVICE ARM" text={`${config.dofPerArm} DOF · force/torque sense`}/><Card code="C" name="TOOL CAROUSEL" text={`${config.toolCount} isolated tools`}/><Card code="D" name="AUTONOMY CORE" text={`${config.autonomyPercent}% delegated policy`}/><Card code="E" name="POWER + THERMAL" text={`${config.batteryKWh} kWh · ${config.radiatorAreaM2} m²`}/></div></section>
<aside className="md-panel md-output"><Title n="05" text="REPAIR READINESS"/><div className={`md-verdict ${result.readiness.toLowerCase()}`}><span>PROCEDURE STATUS</span><b>{result.readiness}</b><small>{result.constraints[0]??"TASK CONTRACT SATISFIED"}</small></div><div className="md-metrics"><Metric label="SERVICE MASS" value={fmt(result.totalMassKg,0)} unit="kg"/><Metric label="STABILITY MARGIN" value={fmt(result.stabilityMargin,2)} unit="×" accent warning={result.stabilityMargin<1}/><Metric label="PAYLOAD MARGIN" value={fmt(result.manipulationMarginKg,0)} unit="kg" warning={result.manipulationMarginKg<0}/><Metric label="REACH MARGIN" value={fmt(result.reachMarginM,1)} unit="m" warning={result.reachMarginM<0}/><Metric label="BATTERY ENDURANCE" value={fmt(result.enduranceHours,1)} unit="h"/><Metric label="THERMAL MARGIN" value={fmt(result.thermalMarginKW,1)} unit="kW" warning={result.thermalMarginKW<0}/><Metric label="FREE-FLIGHT ΔV" value={fmt(result.deltaVMS,1)} unit="m/s"/><Metric label="REPAIR RATE" value={fmt(result.repairCyclesPerDay,2)} unit="cycles/day"/></div><Title n="06" text="POWER + TOOLS"/><Control label="BATTERY" value={config.batteryKWh} unit="kWh" min={1} max={200} step={2} change={v=>update("batteryKWh",v)}/><Control label="ACTIVE LOAD" value={config.activePowerKW} unit="kW" min={.2} max={30} step={.2} change={v=>update("activePowerKW",+v.toFixed(1))}/><Control label="SOLAR AREA" value={config.solarAreaM2} unit="m²" min={.5} max={60} step={.5} change={v=>update("solarAreaM2",v)}/><Control label="RADIATOR" value={config.radiatorAreaM2} unit="m²" min={.2} max={80} step={.5} change={v=>update("radiatorAreaM2",v)}/><Control label="TOOL MAGAZINE" value={config.toolCount} unit="tools" min={1} max={16} step={1} change={v=>update("toolCount",v)}/></aside>
<section className="md-panel md-ops"><div><Title n="07" text="REACTION + CONTACT"/><div className="md-balance"><Metric label="TOOL REACTION" value={fmt(result.operationMomentNm,0)} unit="N·m"/><span>≤</span><Metric label="ANCHORED MOMENT" value={fmt(result.anchorMomentNm,0)} unit="N·m" accent/><i><em style={{width:`${Math.min(100,result.stabilityMargin*50)}%`}}/></i><p>One limb remains attached while another works. A free-floating arm cannot absorb tool torque without rotating the robot and client spacecraft.</p></div></div><div><Title n="08" text="DELAY + AUTONOMY"/><Range label="DELEGATED AUTONOMY" value={config.autonomyPercent} min={0} max={100} suffix="%" change={v=>update("autonomyPercent",v)}/><Control label="COMMAND DELAY" value={config.signalDelayS} unit="s" min={0} max={86400} step={config.signalDelayS>3600?3600:60} change={v=>update("signalDelayS",v)}/><div className="md-autonomy"><span>REQUIRED POLICY</span><b>{fmt(result.requiredAutonomy,0)}%</b><small>{result.autonomyMargin>=0?"LOCAL PROCEDURE AUTHORIZED":"REMOTE CONTROL ARRIVES TOO LATE"}</small></div></div><div className="md-faults"><Title n="09" text="OPEN CONSTRAINTS"/><div>{result.constraints.length?result.constraints.map((x,i)=><article key={x}><span>F-{i+1}</span><p>{x}</p></article>):<article className="clear"><span>PASS</span><p>Two-point contact, tools, energy, heat, reach, and autonomy are inside the preliminary envelope.</p></article>}</div><small>MAINTENANCE ONLY · TORQUE-LIMITED · FAIL-CLOSED RELEASE</small></div></section>
</section></main>}
function RobotDiagram({config,readiness}:{config:MenderConfig;readiness:string}){const reach=115+config.armReachM*22;return <svg className={`md-robot ${config.frame} ${readiness.toLowerCase()}`} viewBox="0 0 720 400" role="img" aria-label="Repair robot physical layout"><defs><marker id="mdArrow" markerWidth="8" markerHeight="6" refX="7" refY="3" orient="auto"><path d="M0 0L8 3 0 6Z" fill="#61d3ad"/></marker></defs><g transform="translate(360 210)"><rect className="body" x="-72" y="-54" width="144" height="108" rx="20"/><circle className="core" r="25"/><g className="eyes"><circle cx="-12" cy="-10" r="4"/><circle cx="12" cy="-10" r="4"/></g><path className="arm anchor" d={`M-60-25Q-${reach*.7}-${reach*.45}-${reach}-62`}/><g className="grip" transform={`translate(-${reach} -62)`}><path d="M0 0l-18-14M0 0l-20 8M0 0l-8 20"/><circle r="8"/></g><path className="arm service" d={`M60-25Q${reach*.72}-${reach*.52} ${reach}-85`}/><g className="tool" transform={`translate(${reach} -85)`}><rect x="-6" y="-15" width="21" height="30"/><path d="M15-8l24-10M15 8l24 10"/></g><path className="arm lower" d={`M-55 35Q-${reach*.6} ${reach*.5}-${reach*.82} 92`}/><circle className="joint" cx="-55" cy="35" r="9"/><circle className="joint" cx="55" cy="-25" r="9"/><circle className="joint" cx="-60" cy="-25" r="9"/>{config.frame==="lunar-crawler"?<><path className="track" d="M-82 53h164l25 34H-107Z"/><circle className="wheel" cx="-60" cy="75" r="19"/><circle className="wheel" cx="60" cy="75" r="19"/></>:config.frame==="rail-walker"?<path className="rail" d="M-145 107H145M-110 95v24M0 95v24M110 95v24"/>:<><circle className="thruster" cx="-82" cy="0" r="10"/><circle className="thruster" cx="82" cy="0" r="10"/></>}<path className="reaction" d="M110-118H225"/><text x="168" y="-126">TOOL REACTION</text><path className="reaction opposite" d="M-110-92H-225"/><text x="-168" y="-100">ANCHOR REACTION</text></g><g className="md-callouts"><path d="M130 292V347H68"/><text x="64" y="361">A // FIXED CONTACT</text><path d="M590 118V61H640"/><text x="644" y="55">B // TOOL HEAD</text><path d="M360 156V71H302"/><text x="298" y="65">C // AUTONOMY CORE</text></g></svg>}
function Title({n,text}:{n:string;text:string}){return <div className="md-title"><span>{n}</span>{text}</div>}function Control({label,value,unit,min,max,step,change}:{label:string;value:number;unit:string;min:number;max:number;step:number;change:(v:number)=>void}){return <div className="md-control"><span>{label}</span><div><button aria-label={`Decrease ${label}`} onClick={()=>change(Math.max(min,value-step))}>−</button><b>{fmt(value,1)}<small>{unit}</small></b><button aria-label={`Increase ${label}`} onClick={()=>change(Math.min(max,value+step))}>+</button></div></div>}function Metric({label,value,unit,accent,warning}:{label:string;value:string;unit:string;accent?:boolean;warning?:boolean}){return <div className={`md-metric ${accent?"accent":""} ${warning?"warning":""}`}><span>{label}</span><b>{value}<small>{unit}</small></b></div>}function Range({label,value,min,max,suffix,change}:{label:string;value:number;min:number;max:number;suffix:string;change:(v:number)=>void}){return <label className="md-range"><span>{label}<b>{value}{suffix}</b></span><input aria-label={label} type="range" min={min} max={max} value={value} onChange={e=>change(Number(e.target.value))}/></label>}function Card({code,name,text}:{code:string;name:string;text:string}){return <article><span>{code}</span><b>{name}</b><small>{text}</small></article>}
